---
const { label, title, blurb, href } = Astro.props;
---
<article class="card">
  <div class="label mono">{label}</div>
  <div>
    <h3>{title}</h3>
    <p>{blurb}</p>
    <a href={href}>Read more</a>
  </div>
</article>
